# Access key from web3forms.com, tied to the destination inbox for the
# contact form. Without this set, the contact form runs in local demo mode
# (shows a success state but doesn't actually send anything).
NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY=
